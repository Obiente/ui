import { defineTheme } from "../../types/theme";

export default defineTheme({
  id: "flat",
  name: "Flat",
  type: "base",
  description:
    "Flat theme with minimal elevation and sharp corners for a modern look.",
  spacing: 1,
  sizing: 1,
  radius: 0.4,
  typography: 1,
  elevation: 0.5,
});
