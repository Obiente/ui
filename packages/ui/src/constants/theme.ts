/**
 * Theme Constants
 * Shared constants for theme system
 */

// Shared theme context key - using string to avoid symbol issues across modules
export const THEME_CONTEXT_KEY = 'obiente-theme';

// Theme persistence key
export const THEME_STORAGE_KEY = 'obiente-theme-preference';
